GetMoviesListResponse:
  properties:
    # The array of movies. updated commitlint
    movies:
      type: array
      items:
        type: object
        properties:
          id:
            type: string
          title:
            type: string
          year:
            type: number
